# What I did
- Changed the existing instance type from t2.micro to t2.nano
- As there you change in requirement by the user

# What I learned
- We can only change the type of instance when it is stopped
- Changing Instance
    - Go in Actions (Right corner after opening a EC2 instance)
    - Then instance settings and change instance type
